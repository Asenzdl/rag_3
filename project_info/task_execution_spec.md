# Task 执行流程规范

> 触发条件：当用户说 **"开始 Task X.X"** 时，读取本文件并按流程执行

---

## 总览：三阶段 + 审核模式

```
阶段1 反思驱动架构设计 → 阶段2 完整代码实现 → 阶段3 技术学习文档
```

审核模式控制阶段间的暂停节奏（用户在触发时指定）：

| 模式 | 行为 | 适用场景 |
|------|------|---------|
| **模式 A**（默认） | 阶段1+2 连续执行 → 暂停确认 → 阶段3 分轮生成 | 平衡效率与质量 |
| **模式 B** | 每个阶段完成后暂停确认 | 想逐阶段审查 |
| **模式 C** | 阶段1+2 连续执行 → 暂停确认 → 阶段3 分轮生成 + 完成后调用 code-reviewer 审查 | 最高质量保障 |

触发示例：`开始 Task 1.5` → 模式A | `开始 Task 1.5 模式B` → 模式B | `开始 Task 1.5 模式C` → 模式C

### 暂停确认的具体形式

- **模式 A**：阶段 2 完成后，AI 输出一行 **「是否进入阶段3」**，等待用户确认后进入阶段 3。

---

## 阶段 1：反思驱动架构设计

### 输入

- 当前 Task 的 outline 文件（`.project/outline/phase_X_*/task_X.X_*.md`）
- `project_info/CONTEXT_INDEX.md`（强制首读，用于路径映射和 Task 目标定位；若会话中已加载则复用）
- 按 `CONTEXT_INDEX.md` 中的 Task 目标判断需读取的 Task 文档（按需加载，用于前瞻性设计和指出过往设计的不足）

### 过程（AI 内部反思，严禁生成 `src/`、`tests/` 下的代码文件）

1. **理解需求**：读取原始 task 需求，理解任务目标、面试知识点、生产级注意事项、验收标准
2. **构思初始方案**：设计模块结构、核心接口、数据流
3. **反思检查**（按顺序，每步不通过则迭代优化方案）：
   - **面试知识点全覆盖？** — 检查 task 中列出的每个面试知识点，确认架构中有对应的设计体现
   - **生产级注意事项全覆盖？** — 检查 task 中的每条注意事项（异常处理、配置管理、性能等），确认架构中有对应方案
   - **10维最佳实践充分落地？** — 根据 CLAUDE.md「质量准则：MUST」中逐维检查，确认每个维度在本 Task 中有具体、可执行的落地方式或**给出豁免声明**
4. **按 `task_doc_design_spec.md` 产出设计**（反思检查全部通过后再动笔）

### 输出

- 存储于 `.project/tasks/phase_X/task_X.X_design.md`
- **核心目标**：深刻理解所有核心架构决策，根据代码蓝图独立编写代码

---

## 阶段 2：完整代码实现

> 对同一文件的多个编辑应合并为尽量少的操作。

- 若实现中发现蓝图有漏洞或需偏离，**必须回阶段 1 更新 design.md 再推进**，不得默默修正
- 直接生成/修改 `src/` 下的代码文件
- 直接生成 `tests/` 下的测试文件，但人工自行测试，AI直接进行下一步！
  - 覆盖正常路径、边界情况、异常路径
- 更新必要的 `__init__.py`（导出公共接口以及提供注释）
- **禁止将代码输出到对话框或写入文档**

---

## 阶段 3：技术学习文档

- 生成 `.project/docs/task_X.X/` 下的技术文档（按当前 Task 编号创建子文件夹）
- 按照 `project_info/tech_doc_design_spec.md` 编写

### 生成节奏

1. **覆盖规划**：在对话中声明文档清单和覆盖范围。**若 design.md 有架构决策章节，每个决策至少被一篇文档覆盖；否则覆盖范围仅限本 Task 的非决策知识点**。最小示例：
   ```
   领航员：整体架构认知（覆盖 D1↔D3 连锁、隐含前提）
   锚点1：Protocol vs ABC（锚定决策 2，展开结构子类型体系）
   锚点2：工厂函数与配置管理（锚定决策 3+4，展开工厂模式演进）
   ```
2. **逐篇生成**：建议先写领航员再写锚点（先建全局框架，避免领航员退化成摘要拼接），但可自行判断方式
3. **深度追问**：写完核心技术解释后、写完全文后各追问一次——追问缺口、接引逃逸、标签伪装；声明任一触发点缺失前必须走三级防逃逸（深度复核 / 主题适配判断 / 禁止线）。详见 `tech_doc_design_spec.md` 深度协议
